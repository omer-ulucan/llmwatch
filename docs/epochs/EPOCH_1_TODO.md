<!-- Epoch 1: Initialization & Infrastructure -->
<!-- 
WHY: This epoch establishes the foundation of the project. We start by initializing the git repository, creating the essential folder structures (backend, frontend, docs), and configuring docker. This structure is essential before any actual logic is written to ensure the multi-container configuration is established correctly from the start.
-->

- [x] Initialize git repository and set up .gitignore
- [x] Create project directory structure for backend, frontend, and docs
- [x] Create docs/epochs/EPOCH_1_TODO.md
- [x] Set up Docker & docker-compose.yml
- [x] Set up .env.example

**Completion Note:** Initialized git, established the base project structure, and successfully set up Docker orchestration and environment configuration for the initial epoch.
